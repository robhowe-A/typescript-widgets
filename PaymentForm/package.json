{
  "name": "paymentform",
  "version": "1.0.0",
  "description": "a payment form demonstrating application-layer encryption",
  "license": "ISC",
  "author": "Robert A. Howell",
  "type": "commonjs",
  "main": "form-output.js",
  "scripts": {
    "minify": "npx terser form-output.js --compress passes=2,drop_console=true,dead_code=true --mangle --comments all -o form-output.min.js&&npx terser form-submit.js --compress passes=2,drop_console=true,dead_code=true --mangle --comments all -o form-submit.min.js&&npx terser secret-encrypt.js --compress passes=2,drop_console=true,dead_code=true --mangle --comments all -o secret-encrypt.min.js&&npx terser output.js --compress passes=2,drop_console=true,dead_code=true --mangle --comments all -o output.min.js&&npx terser secret-decrypt.js --compress passes=2,drop_console=true,dead_code=true --mangle --comments all -o secret-decrypt.min.js"
  }
}
